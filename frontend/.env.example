# URL base de la API (sin /api al final).
# En desarrollo no es necesario (el proxy de Vite lo maneja).
# En producción apunta al servicio de Render.
VITE_API_URL=https://handball-cup-api.onrender.com/api
